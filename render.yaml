services:
  - type: web
    name: task-manager-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn task-manager-backend.main:app --host 0.0.0.0 --port $PORT
    rootDir: . 